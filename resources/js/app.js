import './bootstrap';

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

import IMask from 'imask';

window.IMask = IMask;

import $ from 'jquery'

window.jQuery = window.$ = $